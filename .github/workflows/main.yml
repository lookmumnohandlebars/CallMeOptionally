name: Check Main

on:
  push:
    branches:
      - main
      - workflow-test

jobs:
  test:
    uses: ./.github/workflows/workflow_test.yml

  version:
    uses: ./.github/workflows/workflow_version.yml
